# Source Generated with Decompyle++
# File: is_ship_in_shipyard.pyc (Python 3.11)

if not strategy:
    pass
strategy = BaseStratege()
ship_utils = ship_utils
import common
if not ship_utils.is_ship_from_guild_parent_guild_exists(ship_id_u, strategy = strategy):
    return False
for world_item_id, ship_set in None.iteritems(self.shipyard_ship_uids):
    if ship_id_u in ship_set:
        return True
    return False
