# Source Generated with Decompyle++
# File: on_alliance_pin_del.pyc (Python 3.11)

for data in datas:
    for v in self.union_mark_list:
        if v['id'] == data:
            self.union_mark_list.remove(v)
        return None
