# Source Generated with Decompyle++
# File: get_painting_id_by_ship_record.pyc (Python 3.11)

if not strategy:
    pass
strategy = BaseStratege()
if not ship_record:
    return 100
painting_id = None[ShipField.PAINTING]
if painting_id:
    return painting_id
return None(ship_record[ShipField.SHIP_ID], ship_record[ShipField.USERID], strategy = strategy)
