# Source Generated with Decompyle++
# File: is_enough_capacity_produce_ship_by_ship_id.pyc (Python 3.11)

ship_utils = ship_utils
import common
max_capacity = ship_utils.get_ship_blueprint_max_num(ship_id, bp_record = bp_record, strategy = strategy)
return cur_num + produce_num <= max_capacity
