# 蓝图系统客户端对齐总账

> 日期：2026-07-08
> 范围：计算核心正确性（巅峰/调校/结构分层/单位），不含玩家进度层
> 策略：按反编译客户端源码直接对齐
> 关联：`docs/13`（巅峰调校）、`docs/16`（强化作用域三通道）、`feat/blueprint-client-alignment` 分支

---

## 一、对齐基准来源

通过 3 个反编译源码研究 agent 通读客户端 CPython 3.11 反编译产物（`data/client/decompiled-full-v2.zip`，174557 文件）+ 配置数据实证，逐项验证。客户端权威函数定位：

| 客户端函数 | 模块 | 作用 |
|---|---|---|
| `get_peak_effect(ship_id, peak_level)` | `common.blueprint_utils` | 巅峰 (common, exclusive) 二元组聚合 |
| `is_enhance_influence_effect_value(enhance_id)` | `common.blueprint_utils` | 调校→父强化重定向 |
| `get_enhancement_type(enhance_id)` | `common.blueprint_utils` | 强化三分类 |
| `get_enhancement_max_level(enhance_id)` | `common.blueprint_utils` | 强化/调校等级上限 |
| `get_ship_hp(enhancements, ship_id, ...)` | `common.ship_utils` | 结构值分层装配 |
| `get_blueprint_ex_add_values(bp_record)` | `common.ship_utils` | 技术值→结构绝对值 |
| `calc_effect_add(effect_info, attr_type)` | `data_manager.game_data_utils` | 三通道拆分（docs/16 已白盒） |
| `is_valid_effect(...)` | `data_manager.game_data_utils` | 作用域判定（docs/16 已白盒） |
| `get_weapon_dps_dph_calc` / `AttackCalculator.expression` | `data.ship_attr_calc` | 火力表达式树 |

---

## 二、缺陷清单与修复（5 项）

### 缺陷1【高·已修复】调校(adjust)效果出口错误

**客户端真相**：调校槽（EFFECT_TYPE=2 且有 ADJUST_PROB）有自己的 EFFECT_ID，但属性产出走**父强化** EFFECT_ID 通道。`is_enhance_influence_effect_value` 对调校做重定向：
```python
if enhance_id in SYSTEM_ADJUST_IN_ENHANCE:
    enhance_id = SYSTEM_ADJUST_IN_ENHANCE[enhance_id]   # adjust → 父强化
effect_id = get_enhance_effect_id(enhance_id)
```
然后 `calc_effect_add` 用**父 EID** 查 `cfg_weapon_num_attr` 决定通道。

**数据实证**（2254 调校槽，`cfg_system_enhance` 全表统计）：
- 79%（1780 槽）调校自身 EID == 父 EID（如结构 EID=10）—— 行为不变
- 8%（181 槽）两者不同：调校自身是技能触发器（EID 13005 等，不在 weapon_num_attr），父强化才是真正影响属性的（如 EID 12141 duration ratio_del）
- 59%（1326 槽）两者都是纯技能触发器 —— 正确归零

**典型例**：ST59 调校 `603010140`（自身 EID=13005「快速输出」技能触发器，PARAM=10000）→ 父 `603010110`（EID=12141「缩短冷却与打击时间」，PARAM=80，duration ratio_del）。旧实现把 13005 推进 effectList 被自动过滤为 0 → 调校零产出。

**修复**（`packages/engine/src/data/tuneSystem.ts`）：
- `TuneSlot` 新增 `parentEffect`（父强化 effect，属性产出通道）；`effect` 保留供 UI 显示
- `computeTuneBonus` 用**父 EID** 产 EffectEntry，数值按 calc_effect_add B类公式 `parent.PARAM × cur_level / max_level`（max_level=len(ADJUST_PROB)=10），A类查父 PARAM_LEVEL
- 测试 `tune-enhance-link.test.ts` 更新为正确语义

**commit**：`90b3b7e`

---

### 缺陷2【高·已修复】巅峰 COMMON_EFFECT 变体舰归一化缺失

**客户端真相**（`get_peak_effect`）：
```python
# COMMON (field[0]) 归一化到基础舰
ship_id = (ship_id // 10) * 10 + 1
common_dict = parse(SHIP_PEAK_LEVEL[ship_id][peak_level-1].COMMON_EFFECT)
# EXCLUSIVE (field[1]) 用字面 ship_id
exclusive_dict = parse(SHIP_PEAK_LEVEL[orig_ship_id][peak_level-1].EXCLUSIVE_EFFECT)
return (common_dict, exclusive_dict)
```

**数据实证**：`cfg_ship_peak_level` 共 174 艘船，其中 **85 艘是变体**（如 FG300 的 30102/30103）。变体行 field[0] **恒为空**（1700 行实测），COMMON 数据只存在基础舰行。旧实现用字面 shipId 拼 key → 变体舰查不到 COMMON → 巅峰结构/移速加成全部丢失。

**修复**（`packages/engine/src/data/peakLevel.ts`）：
- 新增 `toMainShipId(shipId)`：`(n//10)*10+1`
- `getPeakSnapshot`：field[0] 用 mainShipId 查表，field[1] 用字面 shipId
- 测试 `peak-variant-normalization.test.ts`：41301基础/41302变体对照，归一化后变体继承基础 COMMON 结构加成(1730)

**commit**：`3112fcd`

---

### 缺陷3【高·已修复】结构分层不符合 get_ship_hp

**客户端真相**（`common.ship_utils.get_ship_hp`）：
```python
ship_hp = get_after_system_effect_value(enhancements, ..., ship_hp_base)  # 强化 EnhanceBasic 作用于 base
ship_hp += get_blueprint_ex_add_values(record)['ship_hp_add']             # 技术值绝对值
final_hp = int(ship_hp + ship_hp_base × Σmodule_EID10_ratio / 100)       # 模块比例作用于裸base，末尾加，不放大
```

**关键点**：模块 EID=10 比例作用对象是**裸 base**，且**不**被系统强化比例放大。

**旧实现（错误）**：
```ts
skeleton = base + moduleBonus
final = floor(skeleton × (1 + 强化比例))   // 把 moduleBonus 也放大了
```

**修复**（`packages/engine/src/data/blueprintResolver.ts`）：
```ts
enhancedBase = floor(base × (1 + 强化比例))         // 强化仅作用于 base
finalStructure = enhancedBase + 巅峰绝对值 + 版本号绝对值 + moduleStructureBonus  // 模块末尾加，不放大
```

**验证**：module+enhance 共存场景（80201 base=278340, 强化14%, module=32484）：
- 旧放大公式 = 354339（偏大 4548 = 模块部分 32484×14%）
- 新对齐公式 = 349791（正确）
- CV3000 锚点（370192，module=0）不受影响；module-structure 锚点（无强化）不受影响

**commit**：`185648b`

---

### 缺陷4【中·已修复】巅峰奖励 EXCLUSIVE 语义订正

**客户端真相**：field[1] 是**巅峰强化奖励（EXCLUSIVE_EFFECT）**，enhanceId 是 EFFECT_TYPE_ENHANCE_ADD（optIdx 70/71），不是"调校"。真正的调校（ADJUST，optIdx 31-43）在 tuneSystem.ts。

**数据实证**：optIdx 70/71 的 EFFECT_TYPE 是 3/5（ENHANCE_EX/ADD），共 606 条。等级直接取 EXCLUSIVE dict 的值。

**修复**：`peakLevel.ts` 顶部注释订正；field[1] 查表用字面 shipId（已在缺陷2修复中落实）。`docs/13` §2.5/§2.8 同步订正。

**commit**：`3112fcd`（注释）+ `185648b`

---

### 缺陷5【中·已审计】单位一致性

**客户端真相**（docs/16 §2.3 + EnhanceBasic.expression）：
- `add_ratio`（effectList 通道）单位是**百分数**（40.0 = 40%），公式 `(100+add_ratio)/100`
- 面板比例字段（结构/命中/闪避/暴击等）按游戏 UI 惯例用**万分比**（1500 = 15.0%）

**审计结论**：模拟器两层单位**各自正确**，非计算 bug：
- `effectList.ts:getEnhanceAdd` 直接累加 e.value 进 addRatio（百分数 ✓，对齐 calc_effect_add）
- `blueprintResolver.ts` 面板字段 `×100` 转万分比（显示口径 ✓，客户端面板同口径）
- `phases/hit.ts` 战斗公式 `base × (1 + bonus)` 用分数（0.15=15%）—— 由调用方转换

**修复**：在 `ResolvedBlueprint` 接口顶部补**单位约定文档**，明确：面板字段=‱（显示）、effectList=%（计算）、结构类=绝对值。避免后续混淆。

**commit**：`185648b`

---

## 三、EFFECT_TYPE / UNLOCK_TYPE 建模（补全）

`rawTypes.ts` 补枚举语义注释（数据实证自 28198 行 `cfg_system_enhance`）：

| 字段 | 值 | 客户端枚举 | 含义 |
|---|---|---|---|
| EFFECT_TYPE | 缺失 | EFFECT_TYPE_ENHANCE | 普通强化项（optIdx 01-11） |
| | 2 | ENHANCE/ADJUST | 调校槽（optIdx 31-43，带 ADJUST_PROB） |
| | 3 | EFFECT_TYPE_ENHANCE_EX | 巅峰奖励 EXCLUSIVE（optIdx 70/71） |
| | 5 | EFFECT_TYPE_ENHANCE_ADD | 巅峰奖励 EXCLUSIVE（optIdx 70/71） |
| UNLOCK_TYPE | 缺失/0 | UNLOCK_TYPE_NONE | 普通槽 |
| | 2 | UNLOCK_TYPE_RE_TECH | 逆向科技/武器技术解锁槽（optIdx 4-12 浮动） |

客户端 `get_enhancement_type` 据此三分类：SYSTEM_ENHANCEMENT / SYSTEM_EXTEND / SYSTEM_ADJUST。

---

## 四、不做的事（明确边界）

按"计算核心正确性"范围，以下**未实装**（客户端有但模拟器不需要/超出范围）：

| 客户端能力 | 不做原因 |
|---|---|
| `get_peak_auth_info`（巅峰授权道具+任务解锁） | 玩家进度层，不影响战斗数值 |
| 多槽位 `SystemScheme` 预设存档（SLOT_INDEX 多套调校） | 存档/UI 层 |
| `gameplay`/`collection` 双表（SHIP_BLUEPRINT_GAME_PLAY 等） | 多人场景路由，单机模拟不需要 |
| `cal_bp_record_use_research_point`（精确技术值会计） | 数据会计层；模拟器由调用方传入 techPoints |
| `get_aircraft_dps`（无人机/载机递归 DPS） | 已部分支持（group_num），递归增强过滤待后续 |

---

## 五、验证状态

| 测试 | 结果 | 说明 |
|---|---|---|
| `tune-enhance-link.test.ts` | 31/31 ✓ | 调校父 EID 路由 + 门控 + 多 EID |
| `tune-anchor.ts` | ✓ | 斗牛级调校槽解析 |
| `peak-variant-normalization.test.ts` | 5/5 ✓ | 变体归一化（新增） |
| `peak-reward.test.ts` | 1/1 ✓ | 巅峰奖励 |
| `cv3000-structure.test.ts` | 5/5 ✓ | CV3000 结构闭环（370192，module=0 不受影响） |
| `module-structure.test.ts` | 8/8 ✓ | 模块结构 + 新增 module+enhance 不放大锚点 |
| `weapon-enhance-tree.test.ts` | 9/9 ✓ | 火力公式核心 |
| `firepower-skill-ratio.test.ts` | 4/4 ✓ | V_SKILL_EFFECT_RATIO + group_num floor（新增） |
| `firepower-anchor.ts` | 16/16 ✓ | 全锺点精确匹配 dump（修复预存坏掉的调用签名） |
| `version-bonus.test.ts` | ✓ | 版本号 |
| 全套 63 测试 | 62/63 | 唯一失败 `panel-comprehensive`（ST59 抵抗/护盾）是**预存问题**，来自工作树中 blueprintCalc.ts 的 getAllModuleEffects 重构，与本对齐工作无关（已 git stash 验证） |

---

## 五·补、火力计算修复（缺陷6/7，2026-07-08 二次审查）

### 缺陷6【高·已修复】computeFirepower 取整方式不符客户端
**客户端真相**（`get_ship_dps_calc` 的 `_add_new_calc`）：每武器 dps 先 `to_int`(floor)，再 Σ，再 ×group_num，末尾 `int()`。
**旧实现**：先 Σ(浮点)，×group_num，最后 `Math.round` → 多武器+大 group_num 偏大。
**修复**（`effectList.ts:computeFirepower`）：每武器 `Math.floor` 后求和，×group_num 后 `Math.floor`。
**验证**：砂龙10701 对舰 旧123→新120（对齐客户端 floor-then-sum）；FG300/ST59/澄海锺点全部精确匹配 dump。

### 缺陷7【中·已修复】V_SKILL_EFFECT_RATIO 占位为 0
**客户端真相**（`get_weapon_attack_calc`）：从武器**模块自带** `cfg_module_effect` 取原值（`get_module_effect_ori_value_info`，不缩放）绑到 V_SKILL_EFFECT_RATIO：
- 所有 dpsType: EFFECT_DAMAGE_INC(12020)
- 对空追加: EFFECT_AIRCRAFT_INC(12062)
- 攻城追加: EFFECT_DESTROY_INC(12060)
**旧实现**：`skillRatio = 0` 占位 → 这3类模块加成全丢。
**修复**（`blueprintCalc.ts` 装配 + `effectList.ts:evalAttack`）：AssembledWeapon 新增 `modDamageInc/modAircraftInc/modDestroyInc`，evalAttack 按 dpsType 组装 skillRatio。
**影响范围**：仅 15 个有此3类模块效果的武器（普通武器如 13011/16031 无此效果 → skillRatio=0，锺点不受影响）。例：13731(船37001) modDestroyInc=25 → 攻城 skillRatio=25。

**★双重绑定已证实为客户端真实行为（2026-07-08 字节码核实）**：
客户端对模块的 12062(对空)/12060(攻城)/12020(通用) PARAM **同时绑两个通道**：
1. **V_ADD_RATIO**：`get_effect_list` 把模块效果收入 `all_effects_list`（is_system_effect=False）→ `get_cur_enhance_add_info` 遍历时**无 is_system_effect 过滤**（源码已读，在 `data.ship_attr_calc.AttrCalcBase.get_cur_enhance_add_info`）→ `calc_effect_add` 对模块走 B类(PARAM×1/1) → 12060/12062 在 weapon_num_attr 是 ratio_add → `add_ratio += PARAM`（disasm 行 716-738 证实）。
2. **V_SKILL_EFFECT_RATIO**：`get_weapon_attack_calc` 的 `skill_effect_ratio += get_module_effect_ori_value_info(module_id, EFFECT_*_INC)`（已读源码）。

两者在 `AttackCalculator.expression` 的 `(100 + V_ADD_RATIO + V_SKILL_EFFECT_RATIO)/100` 里**相加**。这不是反编译伪象——是客户端对空/攻城路径的 intentional 设计（invalid_effect_list 对空只排除 12060、攻城只排除 12062，故意让匹配的那个 EID 留在 V_ADD_RATIO）。

**模拟器对齐**：`resolveBlueprintPanel` 把模块 12020/12060/12062 转 EffectEntry 推进 effectList（V_ADD_RATIO 通道，per-weapon scope），同时 evalAttack 的 skillRatio 也含同值（V_SKILL_EFFECT_RATIO 通道）→ 双绑与客户端一致。
**验证**：`firepower-skill-ratio.test.ts` 测试2 对照 13731 攻城 三态——无绑定/仅skillRatio(25)/双绑(25+25)，双绑 > 仅skillRatio（V_ADD_RATIO 额外放大）。
**注**：这 15 个特殊武器无独立游戏 dump 锺点（13731/74081 等），双绑的精确数值无法 end-to-end 校验，但通道绑定逻辑已字节码证实。

### 附：修复预存坏掉的 firepower-anchor.ts
该脚本因 computeFirepower 签名变更（迁到 effectList.ts 需传 effectList+store 参数）已无法运行。修复调用签名 + 补全手动武器的 aircraftCoef 字段后，**16 个锺点全部精确匹配游戏 dump**（无容差）：
- FG300 13011: 对舰=1542 防空=1851 攻城=324
- ST59 16031/15017/16033: 对舰/攻城全部精确
- 澄海 13124: 防空=1440 / 13122: 防空=375

**引擎 + 编辑器 typecheck**：无新增错误（编辑器仅有预存的 ship-list/EnhanceTree 报错）。

---

## 六、分支与提交

分支 `feat/blueprint-client-alignment`，3 个独立可回退的 commit：
- `90b3b7e` fix(tune): 调校系统按 SYSTEM_ADJUST_IN_ENHANCE 路由重写（缺陷1）
- `3112fcd` fix(peak): COMMON_EFFECT 变体归一化 + EXCLUSIVE 注释订正（缺陷2/4）
- `185648b` fix(structure): 结构分层按 get_ship_hp 重构 + EFFECT_TYPE 建模 + 单位审计（缺陷3/5）

文档：本文件（docs/17）+ docs/13 §2.8 订正。
