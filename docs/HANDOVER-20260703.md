# 交接文档 —— 蓝图编辑器 UI + 蓝图计算层

> 日期：2026-07-03
> 上次交接：58ff0b5（蓝图设计UI: Taro3跨端editor + frida图标提取工具链）

## 一、本次完成的工作

### 1. 路径适配
所有 frida 脚本和文档的硬编码路径从 `E:\星际猎人` / `E:\战斗模拟器` 改为本设备实际路径：
- 游戏客户端：`D:\无尽的拉格朗日`
- 代码仓库：`F:\战斗模拟器\lglrmn`
- git pull 用 ghfast 镜像绕过 dev-sidecar 数据拦截

### 2. 图标提取（frida）
- 从游戏 npk 提取 **391 个图标**（强化图标 + 46 个舰种图标 `icon_ship_type_*.png`）
- 写了 `extract_icons_from_manifest.py`（精准按 manifest 文件名提取）
- 写了 `extract_ship_icons.py`（提取舰种图标）
- manifest 已重新生成

### 3. 舰船列表页重构（packages/editor ship-list）
- **实装过滤**：用 `ship_whitelist.json`（169 艘，用户人工筛选+分类）替代全量 650 艘
- **去 TE 变体**（406 艘）
- **底部舰种图标栏**：9 个舰种（护卫舰/驱逐舰/巡洋舰/战列巡洋舰/战列舰/航母/支援舰/战机/护航艇），点击切换，参照游戏截图
- **站位标签**：从指挥系统的 NAME 提取（前排/中排/后排，战机艇显示往返式/独立式）
- **滚动修复**：ScrollView 改原生页面滚动 + dock fixed

### 4. 蓝图计算层（packages/engine/src/data/blueprintCalc.ts）★新建
独立计算模块，引擎和 UI 共用：

| 函数 | 职责 | 数据源 |
|---|---|---|
| `resolveShipWeapons()` | 武器装配（dph/cd/分类） | cfg_ship_slot + cfg_weapon + cfg_weapon_action |
| `computeFirepower()` | 火力计算（DPM，近似公式） | weapon_action[2]=dph |
| `getBaseDefense()` | **抵抗值+护盾值** | cfg_module_effect（装甲模块效果） |
| `resolveBlueprintPanel()` | 总入口：聚合所有面板数据 | 以上组合 |

### 5. 蓝图主面板（packages/editor blueprint-design）
参照游戏截图布局：
- 顶部：返回按钮 + 指挥值（WEIGHT）
- 舰船名 + 型号名
- 徽章：巅峰等级 / 型号数 / 技术值版本（1.00，留接口）
- 火力区：对舰/防空/攻城（近似值，待校准）
- 基础属性：结构值 / **抵抗值** / **护盾值** / 普通移速 / 曲率速度
- 系统强化状态：每个系统的强化方格上限
- 底部型号切换栏（金色高亮当前型号）

## 二、关键数据发现

### 抵抗值和护盾值的真实来源 ★
**不在** cfg_ship_type（之前用的[8]是错的），**在** `cfg_module_effect`：

```
cfg_ship_slot (cat=0 装甲模块槽)
  → moduleId (CV3000装甲 = 39995)
    → cfg_module_effect["3999501"] = {EFFECT_ID:10033, EFFECT_PARAM:120}  ← 抵抗值
    → cfg_module_effect["3999502"] = {EFFECT_ID:10021, EFFECT_PARAM:15}   ← 护盾值%
```

验证锚点：CV3000 抵抗=120 ✓ 护盾=15% ✓

### 火力计算链路（已定位，未完全突破）
- 计算器类：`data.ship_attr_calc.BlueprintAttrCalc`
- 入口：`calc.get_ship_dps('ship_firepower'/'ship_aa_firepower'/'ship_siege')`
- 内部用 `eval(attr_type)` 机制取属性，直接调用参数难凑
- DPH 来源已确认：`cfg_weapon_action[key][2]`（混沌场 1200/20 精确验证）
- 当前 UI 用近似公式（每分钟伤害），与游戏实际值有偏差，待后续校准
- 详见 `docs/12-蓝图火力计算研究.md`

## 三、待完成 / 下一步

| 优先级 | 任务 | 说明 |
|---|---|---|
| ★★★ | 蓝图强化加点页面 | UI 已有主面板，需做左下角"蓝图强化"入口，展示每个系统的强化项+加点操作 |
| ★★ | 火力计算校准 | FG300锚点：对舰1542/对空1851/攻城324，当前近似公式不准 |
| ★★ | 技术值版本接入 | 加点页面完成后，用 countTechPoints 实时算技术值版本号 |
| ★ | 能力评级 | 数据不存在，需设计算法（暂不做） |
| ★ | 巅峰等级 | 需玩家数据（暂占位） |
| ★ | 舰船2D缩略图 | 无法自动提取（npk文件名hash化，Cocos纹理缓存加载） |

## 四、运行方式

```bash
# 构建
npx tsc -p packages/engine          # 构建引擎
cd packages/editor && npx taro build --type h5  # 构建H5

# 运行(静态服务)
cd packages/editor/dist
node serve.mjs "$(pwd)"             # http://localhost:8080/

# 图标提取(需游戏运行)
py data/client/tools/extract_icons_from_manifest.py
py data/client/tools/extract_ship_icons.py
```

## 五、关键文件索引

- 蓝图计算层：`packages/engine/src/data/blueprintCalc.ts`
- 舰船白名单：`data/client/config/ship_whitelist.json`（169艘，含分类/站位/role）
- 实装shipId：`data/client/config/released_ship_ids.json`（350个基础船型）
- 火力研究：`docs/12-蓝图火力计算研究.md`
- 舰船筛选表：`docs/舰船列表-已筛选完分类好.md`
- 图标提取工具：`data/client/tools/extract_icons_from_manifest.py`、`extract_ship_icons.py`
